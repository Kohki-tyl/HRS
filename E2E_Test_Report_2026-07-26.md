# PR #44 実機テスト結果レポート

## 1. 実施概要

| 項目 | 内容 |
|---|---|
| 実施日 | 2026-07-26 |
| 対象PR | #44 |
| 対象ブランチ | `pr-44-e2e`（`origin/pr-44`） |
| 対象コミット | `e0b1628` |
| OS | Windows |
| Python | 3.11.4 |
| 公開トンネル | ngrok 3.39.8-msix-stable |
| LINE連携 | LINE Messaging API実チャネル・実LINEアプリ |
| 管理画面 | ローカル実ブラウザー、別ブラウザーでも確認 |
| 自動テスト | `pytest`: 71 passed、14 warnings |

秘密情報はリポジトリへ追加せず、Git管理対象外の `.env` から読み込んだ。Webhook URLはngrokの一時HTTPS URLを使用した。

## 2. 総合結果

| 区分 | 結果 |
|---|---:|
| 合格 | 主要な予約・キャンセル・管理画面・一気通貫・永続化・異常系 |
| 不合格 | 1件（チェックアウト完了時の請求額表示） |
| 未実施 | 1件（別LINEアカウントによる所有者分離） |

実機で主要業務フローが動作することを確認した。チェックアウト処理自体は成功するが、完了時に請求額が表示されないため修正が必要である。

## 3. 環境構築・疎通

- [x] `.env` に `LINE_CHANNEL_ACCESS_TOKEN`、`LINE_CHANNEL_SECRET`、`ADMIN_PASSWORD`、`HRS_DB_PATH` を設定
- [x] `.env` が `.gitignore` によりGit追跡対象外であることを確認
- [x] E2E用SQLite DBがGit追跡対象外であることを確認
- [x] `uvicorn main:app --env-file ../.env` で起動
- [x] `GET /health` が `status: ok`、`line_configured: true` を返す
- [x] ngrokでローカルポート8000をHTTPS公開
- [x] LINE Developers ConsoleのWebhook検証が成功
- [x] LINEからのWebhookが署名検証を通過し、`POST /callback` が200を返す
- [x] LINE Bot情報APIへの接続がHTTP 200を返す
- [x] LINE公式アカウントの既定応答を無効化後、HRSから返信される

## 4. LINE予約（UC1）

- [x] `予約` で宿泊日の入力案内が返る
- [x] `7月1日` で日付形式エラーが返る
- [x] 過去日 `2026-07-25` が拒否され、対話を継続できる
- [x] 有効な未来日で空室と料金が表示される
- [x] 正しい部屋選択、宿泊者名、`はい` で予約を確定できる
- [x] 予約番号と合計料金が返る
- [x] `Standard 2, Suite 1` で複数種類・複数室を予約できる
- [x] 複数室の料金が合算される
- [x] 不正な部屋指定 `Standard abc` が拒否され、再入力できる
- [x] 在庫を使い切った日には満室案内が返り、予約確定へ進まない
- [x] 対話途中の `キャンセル` で処理を中断し、予約番号が発行されない
- [x] 宿泊者名 `テスト太郎🌙` が文字化けせず表示される
- [x] 返信は体感で数秒以内で、返信漏れやタイムアウトがない

## 5. LINE予約キャンセル（UC4）

- [x] `予約キャンセル` で予約番号の入力案内が返る
- [x] 所有する予約番号を入力すると予約内容と確認案内が表示される
- [x] `いいえ` でキャンセルを中止し、予約が残る
- [x] `はい` でキャンセルが完了する
- [x] キャンセル済み番号の再入力では「見つかりません」となる
- [x] 存在しない予約番号では予約内容や個人情報を表示しない
- [x] キャンセル後、同日・同室タイプの在庫が復帰する
- [x] 宿泊日当日の予約は期限切れとしてキャンセルを拒否する
- [x] チェックイン済み予約はキャンセルを拒否する
- [ ] 別LINEアカウントから他人の予約番号を入力しても操作できないこと
  - 結果: 別LINEアカウントを用意できず、環境不足で未実施

## 6. 管理者フロントデスク画面

- [x] 未ログイン時にログイン画面が表示される
- [x] 誤ったパスワードではログインできない
- [x] 正しいパスワードでログインできる
- [x] 再読み込み後もログイン状態が維持される
- [x] ログアウト後は管理機能へ戻れず、再ログインが必要になる
- [x] 予約一覧を表示できる
- [x] 予約番号、予約名、宿泊日、部屋番号で検索できる
- [x] 見出し操作で昇順・降順と矢印表示が切り替わる
- [x] 予約詳細モーダルに日付、部屋、料金、状態が表示される
- [x] 本日分予約がチェックイン候補に表示される
- [x] チェックイン時に確認ダイアログが表示される
- [x] チェックイン完了時に部屋番号が表示される
- [x] チェックイン済み予約が候補から消え、チェックアウト候補へ移る
- [x] 部屋番号でチェックアウト候補を検索できる
- [x] チェックアウト時に確認ダイアログが表示される
- [x] チェックアウト処理が成功し、候補から消える
- [x] 予約一覧でチェックアウト済み状態を確認できる
- [x] 現在日時が表示・更新される
- [x] スマートフォン幅でレイアウトと操作性に問題がない
- [x] 別ブラウザーでもログイン、一覧、検索、ソート、詳細、ログアウトを操作できる
- [ ] チェックアウト完了時に請求額が表示される
  - 結果: **不合格**。処理はHTTP 200で完了するが、完了通知は固定文言のみで請求額が表示されない

## 7. LINEと管理画面の一気通貫

- [x] LINEで作成した本日分予約が予約一覧とチェックイン候補へ反映される
- [x] 管理画面でチェックインするとチェックイン候補から消える
- [x] チェックイン済み予約がチェックアウト候補へ移る
- [x] 管理画面でチェックアウトすると候補から消え、一覧がチェックアウト済みになる
- [x] LINEでキャンセルすると同日の在庫が復帰する

## 8. 永続化・DB切替

- [x] サーバー再起動前後で予約件数が6件のまま保持される
- [x] 再起動後も既存予約を管理画面で確認できる
- [x] 再起動後もLINEへ正常に返信できる
- [x] デモ予約が再起動時に重複投入されない
- [x] `HRS_DB_PATH` を一時DBへ変更すると別データで起動する
- [x] 境界テスト後に元DBへ戻すと元の予約6件が復元される
- [x] 一時境界テストDBを削除済み

## 9. 異常系・セキュリティ

- [x] LINE設定なしでは管理画面が200、Webhookが503になる
- [x] 不正な署名のWebhookが400で拒否される
- [x] 管理APIへ認証トークンなしでアクセスすると401になる
- [x] 当日キャンセルとチェックイン済みキャンセルが拒否される
- [x] `.env` の秘密情報をログ、レポート、Git差分へ出力していない

## 10. 検出した不具合

### チェックアウト完了時に請求額が表示されない

- 重要度: 中
- 再現手順:
  1. 本日分予約をチェックインする
  2. チェックアウト候補から対象予約を選択する
  3. 確認ダイアログでチェックアウトを確定する
- 実際の結果: 「チェックアウトが完了しました。」のみ表示され、請求額が表示されない
- 期待結果: 支払い確認に必要な請求額が、確定前または完了時に明確に表示される
- 処理結果: `POST /front/check-out/confirm` は200で、予約状態の更新も成功する
- 原因候補:
  - `ui/static/app.js` がAPI応答後に固定の完了文言だけを表示している
  - `ui/front_desk_terminal.py` のチェックアウト確定結果に請求額が含まれない
- 必要な対応:
  - 確認ダイアログまたは完了通知へ請求額を表示する
  - フロント/APIの自動テストへ請求額表示の検証を追加する

## 11. 未実施項目

### 別LINEアカウントによる予約所有者の分離

別LINEアカウントを用意できなかったため未実施。別アカウントから他人の予約番号を入力した際に「見つかりません」となり、予約内容や存在を推測できる情報が表示されないことを今後確認する。

## 12. 判定

**条件付き合格**

予約、キャンセル、チェックイン、チェックアウト、永続化および主要な異常系は動作した。チェックアウト完了時の請求額表示を修正し、別LINEアカウントを用意できる環境で所有者分離を追加確認する必要がある。
