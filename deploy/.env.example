# Copy this file to deploy/.env and replace the password before deployment.
HOST_PORT=8000
ADMIN_PASSWORD=replace-with-a-long-random-password
HRS_SEED_DEMO=false

# Optional: required only when LINE Messaging API integration is enabled.
LINE_CHANNEL_ACCESS_TOKEN=
LINE_CHANNEL_SECRET=
