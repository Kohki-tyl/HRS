# ToDo

## 完了済み（参考）

- 予約 (UC1) / チェックイン (UC2) / チェックアウト (UC3) / 予約キャンセル (UC4) の実装。
- 永続化の SQLite 統一、空室判定の DB引き。
- 管理者フロントデスク Web 画面（ログイン・予約一覧・チェックイン・チェックアウト）。
- ドメイン・UI・Web エンドポイントの自動テスト。
- 本番起動時の `ADMIN_PASSWORD` 必須化（未設定・既定値では起動を停止）。

## 管理者画面の認証

本番起動時のパスワード検証は実装済み。アプリケーションを単一ワーカーで運用する前提のため、以下は将来対応とする。

- 複数ワーカー構成へ移行する前に、プロセス間で検証可能な署名付きセッション、または共有セッションストアへ管理者トークンを移行する。
- 外部公開時の要件に応じて、ログイン試行回数の制限、セッション有効期限、トークン失効を追加する。
- 管理者認証方式を変更する際に、未認証・ログイン成功/失敗・セッション期限切れ・複数ワーカー相当のケースを自動テストへ追加する。

## 予約キャンセル（UC4）

- 実装済み。機能の詳細・残課題（No-show の処理導線、キャンセル料・返金、予約内容の変更）は [Cancel_Feature.md](../../design/markdown/Cancel_Feature.md) に集約している。

## 技術的負債

- `@app.on_event("startup")` は FastAPI で非推奨。`lifespan` への移行（`scripts/shared/web_frontdesk.py`）。
- 在庫同期は単一プロセス前提。複数ワーカーで厳密化する場合は DB 側の一意制約等を検討。
- ホテル・部屋データが `scripts/startup/main.py` にハードコード（部屋データの登録・管理は要求分析の範囲外）。

## 管理者画面での拡張機能

- チェックイン/チェックアウト処理のログを閲覧可能にする。
